apiVersion: batch/v1
kind: Job
metadata:
  name: {{.Name}}
spec:
  completions: {{.ReplicasMin}}
  parallelism: {{.ReplicasMin}}
  template:
    spec:
      nodeSelector:
        kubernetes.io/arch: arm64
      restartPolicy: Never
      containers:
      - name: job
        image: {{.Image}}